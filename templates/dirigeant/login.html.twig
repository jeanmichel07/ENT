<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Administration</title>
</head>
<style>
    body{
        margin: 0;
        padding: 0;
        font-family: 'Oxygen', sans-serif;
        letter-spacing: 1px;

    }
    section{
        width: 100%;
        height: 100vh;
        background-image: url("/public/images/bg1.jpg");
        background-size: cover;
    }
    .container{
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 700px;
        height: 350px;
        background: #3b91b2;
    }
    .container .form_content, .container .login_form{
        position: relative;
        width: 50%;
        height: 350px;
        float: left;
        box-sizing: border-box;
    }
    .container .form_content{
        padding: 40px 30px;
    }

    .container .form_content a {
        display: inline-block;
        padding: 10px 20px;
        color: #3b91b2;
        background: #ffffff;
        border-radius: 25px ;
        border: 2px solid #ffffff;
        text-decoration: none;
        box-shadow: 0 5px 10px rgba(0,0,0,.2);
    }
    .container .form_content a:hover {
        display: inline-block;
        padding: 10px 20px;
        color: #ffffff;
        background: none;
        border-radius: 25px ;
        border: 2px solid #ffffff;
        text-decoration: none;
        box-shadow: 0 5px 10px rgba(0,0,0,.2);
    }
    .container .login_form{
        background: #FFFFFF;
        height: 380px;
        top: -15px;
        box-shadow: -20px 0 15px rgba(0,0,0,.2);
        padding: 30px;

    }
    .container .login_form h1{
        color: #262626;
        text-align: center;
    }
    .container .login_form input{
        display: block;
        width: 100%;
        padding: 10px;
        box-sizing: border-box;
        margin-bottom: 20px;
        border: 2px solid #ccc;
        border-radius: 25px;
        outline: none;
        font-size: 15px;
    }
    .container .login_form input:hover,.container .login_form input:valid{
        border: 2px solid #3b91b2;
    }
    .container .login_form input[type="submit"]{
        color: #FFFFFF;
        background: #3b91b2;
        font-size: 16px;
        transition: .5s;
        cursor: pointer;
    }
    .container .login_form input[type=submit]:hover{
        background:#ffffff ;
        border: 2px solid #3b91b2;
        color: #3b91b2;
        cursor: pointer;
    }
</style>
<body>

        {% if err is defined %}
            {{ err }}
        {% else %}
            <p></p>
        {% endif %}
        <section>
            <div class="container">
                <div class="form_content">
                    <h2 style="font-size: 17px;margin-bottom: 15px; text-decoration: underline;color: white">DIRIGEANT NL TECHNOLOGIE</h2>
                    <p style="text-align: justify; margin-bottom: 35px; color: #cccccc;text-shadow: 1px 1px 1px #000000">En tant que dirigeant, vous pouvez créer un compte d'administrateur et tous voir les employeurs et leur fonctions.<br>
                    Ici vous pouvez créer votre compte en tant que dirigeant.</p>
                    <a href="{{ path('new_dirigeant') }}">Créer un compte</a>
                </div>
                <div class="login_form">
                    <h1 style="color:#3b91b2; text-shadow: 1px 2px 1px #000000">ADMINISTRATION</h1>
                    {{ form_start(form) }}
                    {{ form_row(form.username) }}
                    {{ form_row(form.password) }}
                    <input type="submit" value="Se connecter" class="btn btn-primary btn-lg btn-block">
                    {{ form_end(form) }}
                </div>
            </div>
        </section>




</body>
</html>